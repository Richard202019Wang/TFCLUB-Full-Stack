The project is about a Full Stack application for a TFCLUB
Using Rest, APIs, Django, db sqlite3 database to create membership, classes, subscription and studios back-end system.
Canada
Using React, APIs, fetch, axios, token, HTML5, CSS ,and Google Maps API to create a font -end for the back-end system. End with perfect Toronto fitness club websites.


For using the application, just run the startup.sh and run.sh to run the whole project on localhost 3000 and localhost 8000 in your terminal.
